# "service" is the name of this project. This will also be added to your AWS resource names.
service: hb-serverless-default
frameworkVersion: '4'

provider:
  name: aws
  runtime: python3.12

functions: # 람다 함수
  serverless: # 람다 함수명
    handler: serverless.test #람다가 구동시킬 핸들러
